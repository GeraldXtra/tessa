{
  "name": "@tessa/protocol",
  "version": "1.0.0",
  "private": true,
  "description": "The Tessa surface/daemon wire contract, in code. SHARED — see CONTRACT.md §7 before editing.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema/*": "./schema/*"
  },
  "files": [
    "src",
    "schema",
    "gen"
  ],
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit"
  }
}
