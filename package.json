{
  "name": "tessa",
  "version": "0.1.0",
  "private": true,
  "description": "TESSA_CORE monorepo — one Python daemon, two surfaces (Console + Orb).",
  "workspaces": [
    "packages/*",
    "apps/console",
    "apps/orb"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "tokens": "node packages/tokens/build.mjs",
    "enums": "node packages/protocol/build-enums.mjs",
    "generate": "npm run tokens && npm run enums",
    "protocol:check": "tsc -p packages/protocol/tsconfig.json --noEmit",
    "protocol:test": "node --experimental-strip-types packages/protocol/test/smoke.ts",
    "contract:check": "node scripts/check-contract.mjs",
    "core:test": "python core/tests/test_contract_sync.py",
    "core:test:auth": "python core/test_auth.py",
    "test": "npm run generate && npm run contract:check && npm run protocol:test && npm run core:test",
    "bootstrap": "npm run generate && npm run test",
    "console:dev": "npm run -w @tessa/console dev",
    "console:build": "npm run -w @tessa/console build",
    "core:dev": "python core/server.py --dev"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "$comment": [
    "apps/orb is owned by a separate Claude Code session. It was deliberately absent from this",
    "list until that session landed it; the Orb session added it on 2026-08-12, exactly as the",
    "note previously here anticipated. Both surfaces now hoist to one shared node_modules.",
    "",
    "Disk budget: the owner's C: has ~14.5 GB free. Keep the dependency set small and prune builds."
  ]
}
